double getSizeFromPercentage(double percentage, double totalSize) {
  return totalSize / percentage;
}

double getSizeFromFlex(double flex, double totalSize) {
  return totalSize / flex;
}

double valueOfPercentage(double value, double percentage) {
  return value * percentage;
}
